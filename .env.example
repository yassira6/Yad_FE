# Optional: a GitHub personal access token the server uses by default when a
# client request doesn't send its own token. Lets everyone on your network
# browse without typing a token in the browser.
GITHUB_TOKEN=

# Port the backend (and, in production, the combined app) listens on.
PORT=8787
